<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bravedroid.favoritesdishes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FavoritesDishes">
        <activity
            android:name=".views.activities.AdderUpdaterDishActivity"
            android:label="Add Dish"
            android:theme="@style/Theme.FavoritesDishes.NoActionBar" />
        <activity
            android:name=".views.activities.SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FavoritesDishes.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.activities.MainActivity" />
    </application>

</manifest>
